// C : evt.target to *dynamically select* and then modify
//     the element where the event happened
//-------------------------------------------------

var formEl = document.querySelector('form')

formEl.addEventListener('submit', function(evt){
  // - Prevent default from reloading the page

  // - Capture values from form <input> els


  // - Check to see if there is user input in the input-fields
  //   ... and give response on the DOM in ouput elements.


})
